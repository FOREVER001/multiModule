<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <android.support.v4.view.ViewPager
        android:id="@+id/viewPager"
        android:layout_above="@+id/bottomnavigation"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
     />
    <android.support.design.widget.BottomNavigationView
        android:layout_alignParentBottom="true"
        android:id="@+id/bottomnavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/app_color_ffffff"
        app:elevation="@dimen/app_dp8"
        app:itemBackground="@color/app_color_ffffff"
        app:itemIconTint="@color/app_selector_item_color"
        app:itemTextColor="@color/app_selector_item_color"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/app_bottomnavagition">
    </android.support.design.widget.BottomNavigationView>
</RelativeLayout>